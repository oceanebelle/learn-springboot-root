version: "3.3"

services:
  mongo:
    image: mongo:4.2.6
    volumes:
      - db-data:/data/db
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: admin
      MONGO_INITDB_DATABASE: admin

volumes:
  db-data: